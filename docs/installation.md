# Installation

## PyPI

```bash
pip install sv-cli
```

## From source

```bash
git clone https://github.com/seovendor/sv-cli.git
cd sv-cli
python -m venv .venv
source .venv/bin/activate
pip install -e '.[dev]'
```

Verify:

```bash
sv --version
sv --help
```
